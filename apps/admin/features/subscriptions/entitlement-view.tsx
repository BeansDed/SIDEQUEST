import type{OperationalRecord}from"@/lib/data/types";import{ResourceTable,StatusBadge}from"@/components/data/resource-table";
export function EntitlementView({entitlements}:{entitlements:OperationalRecord[]}){return <div className="page-stack"><div className="support-note"><strong>Source of truth: payment provider</strong><span>Support can inspect and resync entitlements. Billing changes and refunds stay in the provider dashboard.</span></div><ResourceTable caption="Subscription entitlements" rows={entitlements} columns={[{label:"Member",render:x=><span className="primary-cell"><strong>{x.title}</strong><small>{x.detail}</small></span>},{label:"Term",render:x=>x.meta},{label:"Status",render:x=><StatusBadge status={x.status}/>},{label:"Support action",render:()=> <button className="button">Resync</button>}]} /></div>}
