import{Activity,Database,HardDrive,Radio}from"lucide-react";import type{HealthRecord}from"@/lib/data/types";
const icons=[Database,HardDrive,Radio,Activity];export function HealthDashboard({services}:{services:HealthRecord[]}){return <div className="health-grid">{services.map((service,index)=>{const Icon=icons[index%icons.length];return <article className="health-card" key={service.service}><div><Icon aria-hidden="true"/><span className={`health-dot ${service.status}`}/></div><span className="eyebrow">{service.status}</span><h2>{service.service}</h2><strong>{service.latency}</strong><p>{service.note}</p><small>Error details are redacted from browser views.</small></article>})}</div>}
