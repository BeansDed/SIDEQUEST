"use client";
import { useState } from "react";import{BellRing,CalendarClock,Smartphone}from"lucide-react";
export function CampaignEditor(){const[title,setTitle]=useState("Weekend quest drop");const[body,setBody]=useState("Two new café quests land this Saturday.");const[notice,setNotice]=useState("");return <div className="campaign-layout"><section className="form-section campaign-form"><div className="section-heading"><span>01</span><div><h2>Message & audience</h2><p>Preview before scheduling.</p></div></div><div className="form-grid"><label className="field field-full"><span>Title</span><input value={title} onChange={e=>setTitle(e.target.value)} maxLength={60}/></label><label className="field field-full"><span>Body</span><textarea rows={4} value={body} onChange={e=>setBody(e.target.value)} maxLength={180}/></label><label className="field"><span>City</span><select><option>Metro Manila</option><option>All cities</option></select></label><label className="field"><span>Audience</span><select><option>Active explorers</option><option>Quest regulars</option><option>Plus members</option></select></label><label className="field"><span>Send date</span><input type="date" defaultValue="2026-08-29"/></label><label className="field"><span>Local time</span><input type="time" defaultValue="10:00"/></label>{notice?<div className="success-toast field-full">{notice}</div>:null}<button className="button button-primary field-full" onClick={()=>setNotice("Campaign scheduled. An audit receipt was created.")}><CalendarClock size={14}/>Review & schedule</button></div></section><aside className="phone-preview"><Smartphone aria-hidden="true"/><span className="eyebrow">PUSH PREVIEW</span><div className="push-card"><BellRing aria-hidden="true"/><div><strong>{title||"Campaign title"}</strong><p>{body||"Your message preview appears here."}</p><small>now · SIDEQUEST</small></div></div><p>Delivery uses each explorer’s local time and respects notification preferences.</p></aside></div>}
