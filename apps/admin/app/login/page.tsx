"use client";
import { useState } from "react";import{createClient}from"@supabase/supabase-js";
export default function LoginPage(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[message,setMessage]=useState("");async function signIn(event:React.FormEvent){event.preventDefault();const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){setMessage("Supabase public credentials are not configured.");return}const client=createClient(url,key);const{error}=await client.auth.signInWithPassword({email,password});if(error){setMessage("Sign-in failed. Check your staff account and try again.");return}window.location.assign("/overview")}return <div className="login-wrap"><form className="login-card" onSubmit={signIn}><span className="brand-mark">SQ</span><span className="eyebrow">STAFF ACCESS</span><h1>Welcome back.</h1><p>Use your approved SIDEQUEST staff account.</p><label className="field"><span>Email</span><input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="field"><span>Password</span><input type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{message?<div className="error-banner" role="alert">{message}</div>:null}<button className="button button-primary" type="submit">Sign in</button><small>Access attempts and privileged actions are logged.</small></form></div>}
