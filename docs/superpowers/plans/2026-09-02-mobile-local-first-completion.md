# SIDEQUEST mobile completion implementation plan

1. Add failing tests for theme resolution, compact navigation configuration, lazy/local café fallback, collection management, quest history, review metadata, truthful achievements, and typed deletion.
2. Implement persisted semantic theme context and migrate shared navigation/surfaces and all user routes.
3. Implement lazy café loading plus clearly labelled bundled fallback data.
4. Extend reducer/provider APIs and wire complete collection, quest, review, profile, social, and safety workflows.
5. Replace unsupported external actions with honest local/demo feedback and remove misleading chevrons/buttons.
6. Run Jest, TypeScript, lint, Android build/debug checks, and an emulator walkthrough including restart persistence.
